#include <iostream>
using namespace std;


int sum(string s, int i){
  if(i==s.size()-1) return s[i]-48;
  return s[i]-48+sum(s, i+1);
}

int main(){
    string s;
    cin>>s;
    
    cout<<sum(s, 0);
}
